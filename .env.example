# ===========================================
# WrocDevs Environment Configuration
# ===========================================

# Database Connection
# For local development with Docker:
DATABASE_URI=postgresql://postgres:postgres@localhost:5432/payload
# For production (set in server .env):
# DATABASE_URI=postgresql://postgres:SECURE_PASSWORD@postgres:5432/payload

# Payload CMS Secret (minimum 32 characters)
# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=YOUR_SECRET_HERE_MIN_32_CHARS

# Public URL (no trailing slash)
# Development:
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Production:
# NEXT_PUBLIC_SERVER_URL=https://wrocdevs.com

# ===========================================
# Optional Configuration
# ===========================================

# Cron Job Authentication
# Generate with: openssl rand -hex 16
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Draft Preview Authentication
# Generate with: openssl rand -hex 16
PREVIEW_SECRET=YOUR_PREVIEW_SECRET_HERE

# ===========================================
# Brevo Newsletter Integration
# ===========================================
# Get your API key from: Settings > SMTP & API > API Keys
BREVO_API_KEY=xkeysib-your-api-key

# Your list ID from: Contacts > Lists
BREVO_LIST_ID=your-list-id

# Your double opt-in template ID from: Campaigns > Templates
BREVO_DOI_TEMPLATE_ID=your-template-id

# ===========================================
# Production Only (docker-compose.prod.yml)
# ===========================================
# Database password for PostgreSQL container
# Generate with: openssl rand -hex 16
# DB_PASSWORD=YOUR_SECURE_DB_PASSWORD
